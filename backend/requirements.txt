# --- Web Framework & Core ---
# Core web server framework for API endpoints
Flask==3.1.3
# Loads environment variables from .env files
python-dotenv==1.2.2

# --- Database Integration ---
# Flask wrapper for MongoDB connection handling
Flask-PyMongo==2.3.0
# Core MongoDB driver for Python
pymongo==4.11.3

# --- Security & Authentication ---
# JWT token generation, parsing, and management for user sessions
Flask-JWT-Extended==4.7.1
# Cryptographic hashing library used for securely storing user passwords
bcrypt==4.2.1
# Rate limiting for API endpoints to prevent brute-force and DDoS attacks
Flask-Limiter==3.5.0
# Encryption/decryption library used for securing biometric face embeddings
cryptography==46.0.7

# --- API Utilities ---
# Handles Cross-Origin Resource Sharing (CORS) for frontend-backend communication
Flask-CORS==6.0.0

# --- Computer Vision & Machine Learning ---
# Core image processing library (headless version avoids unnecessary GUI dependencies)
opencv-python-headless==4.11.0.86
# High-performance matrix math (used heavily by computer vision tools)
numpy==1.26.4
# Scientific computing used for advanced array/image operations
scipy==1.14.1
# Convenience functions for basic image processing like resizing and rotations
imutils==0.5.4
# Google's ML framework used for extremely fast primary face detection
mediapipe==0.10.21
# Pre-trained deep learning models for converting face crops into 512D embeddings
keras-facenet==0.3.2
# Core machine learning backend required to run keras-facenet models
tensorflow==2.16.2

# --- Document & File Processing ---
# Standard Python image library for image manipulations
Pillow==12.2.0
# Optical Character Recognition (OCR) fallback/utilities
pytesseract==0.3.13
# Reads and writes Excel (.xlsx) files for bulk data exports (e.g. attendance matrices)
openpyxl==3.1.5
# PDF generation for academic reports and structured data exports
reportlab==4.4.10

# --- Background Jobs & Caching ---
# In-memory data store client for queue management and rapid lookups
redis==5.2.1

# --- Observability, Logging & Production ---
# Production-grade WSGI HTTP Server
gunicorn==23.0.0
# Formats logs in structured JSON format for easier aggregation
python-json-logger==2.0.7
# Exposes application metrics for Prometheus scraping (monitoring)
prometheus-client==0.19.0
# Application error tracking and crash reporting
sentry-sdk==2.8.0

# --- Communications ---
# Email client for sending OTPs, password recovery, and system notifications
yagmail==0.15.293
